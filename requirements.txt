# requirements.txt (Cleaned)
django==5.1.7
gunicorn
whitenoise
sqlparse
tzdata
django-modeltranslation
django-recaptcha
django-widget-tweaks
django-ckeditor==6.7.1

# pillow # Add if needed by your project or other dependencies
# python-dotenv # Add if using .env files
# psycopg2-binary # Add ONLY if using PostgreSQL